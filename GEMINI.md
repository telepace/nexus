# Nexus 项目上下文与协作指南

本项目配置了多个模型上下文协议 (MCP) 服务，可以通过 `@` 符号在提问时引用它们，以便为我（AI助手）提供即时、准确的上下文信息。

## 可用的 MCP 服务

以下是当前项目中已配置的服务列表及其用途：

### `@API Document`
- **功能**: 提供项目在 Apifox 上的 API 文档。
- **使用场景**: 当你需要查询、理解或基于项目后端 API 进行开发时。
- **示例**: `@API Document 列出所有和用户管理相关的接口。`

### `@postgres`
- **功能**: 连接到项目的 PostgreSQL 数据库。
- **使用场景**: 当你需要查询数据库表结构、数据或生成 SQL 语句时。
- **示例**: `@postgres "users" 表的结构是怎样的？`

### `@github`
- **功能**: 提供当前 GitHub 仓库的相关信息。
- **使用场景**: 查询 issues、拉取请求(PRs)、提交历史等。
- **示例**: `@github 最近的3次代码提交是什么？`

### `@amap-maps`
- **功能**: 提供高德地图的地理信息服务。
- **使用场景**: 需要进行地理位置计算、路线规划或查询时。
- **示例**: `@amap-maps 查询从北京到上海的驾车距离。`

### `@sequential-thinking`
- **功能**: 帮助将复杂问题分解为有序的步骤。
- **使用场景**: 当你需要一个清晰的、按部就班的计划来完成一个复杂任务时。
- **示例**: `@sequential-thinking 规划一下为项目添加用户认证功能的步骤。`

---

## 协作规则

为了更高效、更安全地协作，请遵循以下规则：

### 1. Git 分支隔离 (Git Branching)
在开始任何一系列的代码修改之前，我会检查当前的 Git 分支。如果在一个主分支（如 `main`, `master`）上，我会提议并请求您的许可来创建一个新的特性分支，以确保所有修改都在隔离的环境中进行。请在我忘记时提醒我。

### 2. 测试驱动开发 (TDD)
当进行功能开发或修改时，我将遵循测试驱动开发（TDD）的模式。具体流程如下：
1.  **编写测试**: 首先，我会创建一个新的测试文件或在现有测试文件中添加新的测试用例，这个测试会描述新功能的目标状态或复现一个 Bug。这个测试在初始阶段应该是失败的。
2.  **编写/修改代码**: 接着，我会编写最少的代码来让这个测试通过。
3.  **重构**: 在测试通过后，我会对代码进行重构以提高代码质量，并确保所有测试仍然通过。
