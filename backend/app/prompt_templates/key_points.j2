你是一个信息提取专家。请从以下内容中提取最重要的关键要点。

## 内容信息
{% if document_metadata.title %}
**标题**: {{ document_metadata.title }}
{% endif %}
{% if document_metadata.domain %}
**领域**: {{ document_metadata.domain }}
{% endif %}

## 分析对象
系统消息中已提供完整的文章内容，请仅基于该文章提取关键要点。

{% if context_history and context_history|length > 0 %}
## 上下文信息
{% for item in context_history %}
- **{{ item.operation_type|title }}**: {{ item.output_summary|truncate(100) }}
{% endfor %}

**注意**: 在现有分析基础上提取补充性的关键要点。
{% endif %}

{% if extraction_criteria and extraction_criteria|length > 0 %}
## 提取标准
{% for criterion in extraction_criteria %}
- {{ criterion }}
{% endfor %}
{% endif %}

## 要求
1. 提取5-8个最重要的要点
2. 每个要点简洁明了，突出核心价值
3. 按重要性排序
4. 涵盖核心概念、实用方法、关键数据、独特见解
5. **严格使用markdown格式**：
   - 每个要点用 **粗体** 作为标题，后跟冒号
   - 使用 *斜体* 标注重要术语、技术名词、工具名称
   - 使用 **粗体** 强调核心概念和关键数字
   - 保持列表格式的一致性，每行一个要点
   - 适当添加简要说明，但保持简洁
   - 避免过度嵌套的格式结构
{% if context_history %}6. 重点关注之前分析中未涉及的新要点{% endif %}
7. 输出内容必须是纯markdown格式，不包含任何JSON结构

## 输出格式
请严格按照以下markdown格式输出，不要添加任何额外的结构：

## 关键要点

- **要点1标题**: 具体描述内容，包含*关键术语*和**重要概念**
- **要点2标题**: 具体描述内容，突出**核心数据**或*技术名词*
- **要点3标题**: 具体描述内容，说明实际应用价值和*实施方法*
- **要点4标题**: 具体描述内容，提供**关键数据**或案例分析
- **要点5标题**: 具体描述内容，展现独特见解或*创新观点*

{% if context_history %}
## 补充信息

- **关键数据**: [重要的数字或统计信息，使用**粗体**强调数值]
- **实用建议**: [可操作的建议或方法，标注*工具名称*]
- **延伸思考**: [值得进一步探索的方向，突出**核心价值**]
{% endif %}