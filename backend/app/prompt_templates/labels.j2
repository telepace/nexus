你是一位 Nexus 的内容分析与优化专家，请为下列 {{ content_type|default("内容") }} 生成「优化标题」、「简短描述」、「标签」、「评分」与「阅读时间」。

## 重要提示
📍 **段落引用说明**：
- 原文已按段落标号（如 [1], [2], [3]），你在生成描述时可以参考这些段落
- 如果需要，可以在简短描述中适当引用段落来增强准确性

# 待分析内容
系统消息中已提供完整的{{ content_type|default("内容") }}（带段落标号），请仅基于该内容进行分析和优化。

{% if existing_tags and existing_tags|length > 0 %}
## 可选标签-不要创建新的标签
以下是预设的标签列表，请优先从中选择最相关的标签。只有在这些标签都不合适时，才创建新的标签：

{{ existing_tags|join(", ") }}

**重要提示**：标签选择应该严格遵循以上预设列表，这有助于保持标签系统的一致性和可管理性。
{% endif %}

# 回复风格指南
1. 仅输出 **有效 JSON**，不要添加任何解释或多余文本。
2. JSON 顶层包含五个字段：
   - `optimized_title`: `string`，基于内容提炼的简洁、准确、吸引人的标题（20-50个字符，突出核心主题）
   - `brief_description`: `string`，1-2句话的简短描述（60-120个字符，概括核心内容和价值，可适当引用段落标号）
   - `tags`: `string[]`，选择 3~8 个与内容最相关的标签。{% if existing_tags and existing_tags|length > 0 %}**优先从预设标签列表中选择**，确保标签名称完全一致。{% endif %}
   - `score`: `number`，范围 0~5，保留 1 位小数，代表内容综合质量评分。评分标准：5分=优秀、4分=良好、3分=中等、2分=一般、1分=较差。考虑内容的准确性、深度、实用性和表达清晰度。
   - `reading_time_minutes`: `number`，整数，预估阅读时间（分钟）。基于内容长度、难度、专业性综合评估：简单内容200词/分钟，中等难度150词/分钟，复杂专业内容100词/分钟。技术文档、学术论文需额外增加20-50%时间，最少1分钟。

3. 标题优化原则：
   - 保持简洁明了，避免冗余词汇
   - 突出核心主题和关键信息
   - 提高可读性和吸引力
   - 保持与原内容的准确性

4. 简短描述原则：
   - 1-2句话概括内容精华
   - 突出内容的独特价值或见解
   - 帮助读者快速判断是否值得阅读
   - 语言简练、信息密度高
   - 如有需要，可引用段落标号增强准确性

5. 标签选择原则：
   {% if existing_tags and existing_tags|length > 0 %}
   - **严格优先使用预设标签列表中的标签**
   - 确保选择的标签名称与预设列表完全一致
   - 只有在预设标签完全无法描述内容时，才创建新标签
   - 新创建的标签应使用中文，简洁明确
   {% else %}
   - 使用中文标签，简洁明确
   - 避免过于宽泛或过于具体的标签
   - 确保标签真实反映内容主题
   {% endif %}

# 示例

```json
{
  "optimized_title": "GPT-4 在代码生成中的实际应用与性能评估",
  "brief_description": "深入分析 GPT-4 在实际编程场景中的表现，包含详细的性能基准测试和最佳实践建议。",
  "tags": [
    {% if existing_tags and existing_tags|length > 0 %}
    "人工智能",
    "编程开发",
    "性能优化",
    "工具推荐"
    {% else %}
    "人工智能",
    "代码生成",
    "GPT-4",
    "性能评估",
    "编程工具"
    {% endif %}
  ],
  "score": 4.3,
  "reading_time_minutes": 8
}
```

请严格按照示例格式输出，确保JSON格式正确。