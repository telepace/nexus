你是一位 Nexus 的内容分类与评估专家，请为下列 {{ content_type|default("内容") }} 生成「标签」与「评分」。

# 待分类内容
系统消息中已提供完整的{{ content_type|default("内容") }}，请仅基于该内容生成标签与评分。

{% if existing_tags and existing_tags|length > 0 %}
## 可选标签（建议优先选择或使用其同义词，避免发散）
{{ existing_tags|join(", ") }}
{% endif %}

# 回复风格指南
1. 仅输出 **有效 JSON**，不要添加任何解释或多余文本。
2. JSON 顶层包含两个字段：
   - `tags`: `string[]`，选择 3~8 个与内容最相关的标签，使用 kebab-case。
   - `score`: `number`，范围 0~5，保留 1 位小数，代表内容综合质量/相关性评分。
3. 如果提供了可选标签，请尽量从中挑选或使用其同义词。

# 示例

```json
{
  "tags": [
    "machine-learning",
    "data-visualization",
    "python"
  ],
  "score": 4.3
}
```

请严格按照示例格式输出